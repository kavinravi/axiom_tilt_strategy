# Lean runtime for the VPS trading box (no CUDA torch / research stack).
# Covers: weights (Fri), rebalance (Mon), publish (15-min + EOD), tests.
# Install: .venv/bin/pip install -r deploy/requirements-vps.txt

# Core data
numpy>=1.26
pandas>=2.2
pyarrow>=16

# Strategy model + utils import chain
lightgbm>=4.5
scikit-learn>=1.5
joblib>=1.3
gymnasium>=1.0
pyyaml>=6.0

# Data sources
python-dotenv>=1.0
pandas-datareader>=0.10
Nasdaq-Data-Link>=1.0
yfinance>=0.2
requests>=2.32

# Execution + dashboard
ib_async>=1.0
supabase>=2.0

# Validation on the box
pytest>=8
